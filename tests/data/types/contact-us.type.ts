/** Fields for the Contact Us form plus a local file path for upload tests. */
export interface ContactUsInfo {
    name: string,
    email: string,
    subject: string,
    message: string,
    filePath: string
}